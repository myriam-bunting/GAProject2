import React from "react";

const Results = (props) => {
  return (
    <div>
      {/* {props.results.map((i) => {
        return i.fields.name.stringValue;
      })} */}
    </div>
  );
};

//return level from the test to display
export default Results;
